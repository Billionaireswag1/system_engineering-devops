#!/usr/bin/env bash
# Stops the running 4-to_infinity_and_beyond process by killing its PID silently

pids=$(pgrep -f 4-to_infinity_and_beyond)

if [ -z "$pids" ]; then
  exit 0
fi

for pid in $pids; do
  kill "$pid"
done
